% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-mae.R
\name{read_mae_fast}
\alias{read_mae_fast}
\title{Read Maestro (.mae/.maegz) Files}
\usage{
read_mae_fast(
  path,
  properties = NULL,
  strip_prefix = TRUE,
  include_atoms = FALSE
)
}
\arguments{
\item{path}{Character. Path to a \code{.mae} or \code{.maegz} file, or a directory
(reads all \code{.mae}/\code{.maegz} files in it).}

\item{properties}{Character vector or \code{NULL}. If specified, only extract
these properties (matched after prefix stripping if \code{strip_prefix} is
\code{TRUE}). \code{NULL} extracts all CT-level properties.}

\item{strip_prefix}{Logical. If \code{TRUE} (default), strip m2io type and
namespace prefixes from property names (e.g., \code{r_i_glide_gscore}
becomes \code{glide_gscore}).}

\item{include_atoms}{Logical. If \code{TRUE}, include atom data as a
list-column named \code{atoms}. Default is \code{FALSE}.}
}
\value{
A tibble where each row is one CT (Connection Table) entry.
Columns are the extracted properties, plus \verb{_ct_index} (integer)
and \verb{_source_file} (character) tracking provenance.
}
\description{
Parse Schrodinger Maestro format files natively in R without requiring
a Schrodinger installation. Extracts CT (Connection Table) level
properties into a tidy tibble.
}
\examples{
mae_file <- system.file("extdata", "example.mae",
  package = "rschrodinger", mustWork = TRUE)
read_mae_fast(mae_file)

}
